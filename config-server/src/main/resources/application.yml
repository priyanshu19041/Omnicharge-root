server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/priyanshu19041/Omnicharge-root
          searchPaths: config-repo
          clone-on-start: true

# To see more details if anything fails
logging:
  level:
    org.springframework.cloud.config: DEBUG
